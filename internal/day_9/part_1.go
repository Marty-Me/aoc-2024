package day7

import (
	"bufio"
	"fmt"
	"strings"
)

type Vector2 struct {
	x int
	y int
}

func SolvePartOne(input string) string {
	_ = bufio.NewScanner(strings.NewReader(input))
	return fmt.Sprint(0)
}
